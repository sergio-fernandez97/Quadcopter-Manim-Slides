This directory mirrors the project-local `.claude/` configuration for Codex workflows.

- `agents/`, `commands/`, `hooks/`, `skills/`, and `agent-memory/` were copied from `.claude/`.
- Internal hook and memory paths were retargeted from `.claude/` to `.codex/`.
- Repo-wide instructions still live at the root in `AGENTS.md`.
